name: Build and Push Adhan API Docker Image

on:
  push:
    branches:
      - main
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the repo
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Restore cached cities.db
      - name: Restore cached cities.db
        id: restore-db
        uses: actions/cache@v4
        with:
          path: backend/src/data/cities.db
          key: cities-db-v1

      # 3️⃣ Install Python & uv for data_tools
      - name: Set up Python & uv
        run: |
          python -m pip install --upgrade pip
          curl -LsSf https://github.com/astral-sh/uv/releases/download/0.8.19/uv-installer.sh | sh
          export PATH="$HOME/.local/bin:$PATH"
          uv --version

      # 4️⃣ Generate cities.db if missing
      - name: Generate cities.db (if cache miss)
        if: steps.restore-db.outputs.cache-hit != 'true'
        run: |
          echo "cities.db not found in cache — building it..."
          cd data_tools
          uv sync --locked --no-install-project --no-dev
          uv run python main.py \
          --download https://download.geonames.org/export/dump/allCountries.zip \
          --output ../backend/src/data/cities.db
          ls -lh ../backend/src/data

      # 5️⃣ Upload cities.db artifact (optional backup)
      - name: Upload cities.db artifact
        if: steps.restore-db.outputs.cache-hit != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: cities-db
          path: backend/src/data/cities.db
          retention-days: 30

      # 6️⃣ Set up QEMU for multi-arch Docker
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # 7️⃣ Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 8️⃣ Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      # 9️⃣ Extract version from pyproject.toml
      - name: Extract version
        id: version
        run: |
          version=$(grep -Po '(?<=^version = ")[^"]*' backend/pyproject.toml)
          echo "VERSION=$version" >> $GITHUB_ENV

      # 🔟 Build & push multi-arch Docker image
      - name: Build and push Docker image
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm64,linux/arm/v7 \
            --cache-from=type=registry,ref=aminekun90/adhan-api:cache \
            --cache-to=type=registry,ref=aminekun90/adhan-api:cache,mode=max \
            -f backend/DOCKERFILE \
            -t aminekun90/adhan-api:latest \
            -t aminekun90/adhan-api:${VERSION} \
            --push \
            .
